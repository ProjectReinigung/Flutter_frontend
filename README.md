# Cleaning Manager Frontend

Flutter frontend for the Cleaning Manager application.

## Local Development

```sh
flutter pub get
flutter run
```

Local builds use localhost service URLs by default. Production builds are configured with Dart defines in the GitHub Actions workflows.

Deployment instructions are in [DEPLOYMENT.md](DEPLOYMENT.md).
